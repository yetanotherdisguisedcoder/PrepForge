---
title: "Linux Operations"
category: os
part: "PART 6 — OPERATING SYSTEMS"
number: "6.5"
order: 6.5
priority: P1
status: not-started
source: 00_MASTER_TOPIC_LIST.md
note: "you'll be asked \"server is slow, what do you do?\""
---

- [ ] The USE method: Utilization, Saturation, Errors
- [ ] `top`/`htop`, `vmstat`, `iostat`, `mpstat`, `pidstat`, `free`, `df`/`du`, `uptime`, load average (and what load average actually means on Linux — includes uninterruptible sleep)
- [ ] `ps`, `lsof`, `strace`, `ltrace`, `perf`, flame graphs
- [ ] `netstat`/`ss`, `tcpdump`, `dig`/`nslookup`, `curl -v`, `traceroute`, `mtr`, `telnet`/`nc`
- [ ] `journalctl`, `systemctl`, unit files, log rotation
- [ ] `grep`/`awk`/`sed`/`jq`/`xargs`/`find` — one-liner fluency
- [ ] Shell scripting basics, exit codes, `set -euo pipefail`, trap
- [ ] Cron & systemd timers; environment differences in cron
- [ ] File permissions & ownership in containers; running as non-root
- [ ] SSH keys, agent forwarding, bastion hosts, and **why SSM Session Manager beats SSH** (your CloudNap design decision)

---
